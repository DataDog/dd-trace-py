rulesets:
  - python-best-practices:
    rules:
      comment-fixme-todo-ownership:
        ignore:
          - "**"
      max-function-lines:
        ignore:
          - "tests/ci_visibility/api/fake_runner_*.py"
      nested-blocks:
        ignore:
          - "**"
