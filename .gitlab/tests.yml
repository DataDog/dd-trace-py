stages:
  - setup
  - riot
  - exploration

variables:
  RIOT_RUN_CMD: riot -P -v run --exitfirst --pass-env -s
  REPO_LANG: python # "python" is used everywhere rather than "py"
  # CI_DEBUG_SERVICES: "true"

include:
  - local: ".gitlab/services.yml"
  - local: ".gitlab/testrunner.yml"


# Do not define a `needs:` in order to depend on the whole `precheck` stage
.test_base_riot:
  extends: .testrunner
  stage: riot
  needs: [ build_base_venvs, prechecks ]
  parallel: 4
  services:
    - !reference [.services, ddagent]
  # DEV: This is the max retries that GitLab currently allows for
  before_script:
    - !reference [.testrunner, before_script]
    - unset DD_SERVICE
    - unset DD_ENV
    - unset DD_TAGS
    - unset DD_TRACE_REMOVE_INTEGRATION_SERVICE_NAMES_ENABLED
  script:
    - |
      hashes=( $(.gitlab/scripts/get-riot-hashes.sh "${SUITE_NAME}") )
      echo "NIGHTLY_BUILD: ${NIGHTLY_BUILD}"
      if [[ ${#hashes[@]} -eq 0 ]]; then
        echo "No riot hashes found for ${SUITE_NAME}"
        exit 1
      fi
      for hash in "${hashes[@]}"
      do
        echo "Running riot hash: ${hash}"
        riot list "${hash}"
        ${RIOT_RUN_CMD} "${hash}" -- --ddtrace
      done
      ./scripts/check-diff ".riot/requirements/" \
        "Changes detected after running riot. Consider deleting changed files, running scripts/compile-and-prune-test-requirements and committing the result."
      ./scripts/check-diff "scripts/integration_registry/registry.yaml" \
        "Registry YAML file (scripts/integration_registry/registry.yaml) was modified. Please run: scripts/integration_registry/update_and_format_registry.py and commit the changes."


.test_base_riot_snapshot:
  extends: .test_base_riot
  services:
    - !reference [.test_base_riot, services]
    - !reference [.services, testagent]
  before_script:
    - !reference [.test_base_riot, before_script]
    # DEV: All job variables get shared with services, setting `DD_TRACE_AGENT_URL` on the testagent will tell it to forward all requests to the
    # agent at that host. Therefore setting this as a variable will cause recursive requests to the testagent
    - export DD_TRACE_AGENT_URL="http://testagent:9126"
    - ln -s "${CI_PROJECT_DIR}" "/home/bits/project"

.test_base_riot_gpu:
  extends: .test_base_riot
  image: !reference [.testrunner_gpu, image]
  tags: !reference [.testrunner_gpu, tags]
  timeout: 40m
  parallel: 2
  variables:
    KUBERNETES_MEMORY_REQUEST: "12Gi"
    KUBERNETES_MEMORY_LIMIT: "12Gi"
    KUBERNETES_CPU_REQUEST: "2"
    KUBERNETES_CPU_LIMIT: "2"
  before_script:
    - !reference [.testrunner_gpu, before_script]
    - !reference [.test_base_riot, before_script]

.test_base_riot_gpu_snapshot:
  extends: .test_base_riot_gpu
  services:
    - !reference [.test_base_riot_gpu, services]
    - !reference [.services, testagent]
  before_script:
    - !reference [.test_base_riot_gpu, before_script]
    - export DD_TRACE_AGENT_URL="http://testagent:9126"
    - ln -s "${CI_PROJECT_DIR}" "/home/bits/project"

# ---------------------------------------------------------------------------
# ddtest job templates. Used by gen_gitlab_config.py for suites with
# `ddtest: true` in suitespec. ddtest owns plan/run + file splitting; riot
# knowledge stays in Riot's command override. See docs/ddtest-dogfooding.md
# for the full design.
#
# before_script and script share the same shell session in GitLab. Venv
# preparation and activation are handled by Riot for each command.
# ---------------------------------------------------------------------------
.ddtest_build:
  extends: .testrunner
  stage: setup
  needs: [prechecks]
  script:
    - |
      set -e -o pipefail
      mkdir -p bin
      # Build ddtest from main (merged but not yet released).
      curl -fsSL https://go.dev/dl/go1.26.5.linux-amd64.tar.gz | tar xz -C /tmp
      export PATH="/tmp/go/bin:$PATH"
      rm -rf /tmp/ddtest-src
      git clone --depth 1 https://github.com/DataDog/ddtest.git /tmp/ddtest-src
      (cd /tmp/ddtest-src && go build -o "${CI_PROJECT_DIR}/bin/ddtest" .)
      chmod +x bin/ddtest
      # Riot's --command override supplies the venv environment. Install the
      # dependencies from the locked file, then overlay the pinned Git commit.
      python -m venv bin/riot-env
      bin/riot-env/bin/python -m pip install --no-cache-dir -r ci/requirements/ci.txt
      # VCS requirements cannot be hash-verified by pip; the commit itself is
      # pinned and dependencies are supplied by the locked requirements above.
      # ci-deps: allow-no-version
      bin/riot-env/bin/python -m pip install --no-cache-dir --no-deps \
        "riot @ git+https://github.com/DataDog/riot.git@ffafc0689be524643c7f77c82e536caceea98e9e"
  artifacts:
    paths:
      - bin/ddtest
      - bin/riot-env
    expire_in: 1 week

.ddtest_base:
  extends: .testrunner
  services:
    - !reference [.services, ddagent]
  before_script:
    - !reference [.testrunner, before_script]
    - unset DD_SERVICE
    - unset DD_ENV
    - unset DD_TAGS
    - unset DD_TRACE_REMOVE_INTEGRATION_SERVICE_NAMES_ENABLED

.ddtest_base_snapshot:
  extends: .ddtest_base
  services:
    - !reference [.ddtest_base, services]
    - !reference [.services, testagent]
  before_script:
    - !reference [.ddtest_base, before_script]
    - ln -s "${CI_PROJECT_DIR}" "/home/bits/project"

# Plan: discover the venv's suite files and write .testoptimization/.
# No snapshot variant — plan only collects (glob + Datadog API) and sends
# no traces, so the testagent is never needed at plan time.
# --tests-location is a raw glob (ddtest does not combine it with python_files),
# so each Riot venv declares its complete location in DDTEST_TESTS_LOCATION.
.ddtest_plan:
  extends: .ddtest_base
  script:
    - |
      export DDTEST_PLAN_AGENT_URL="${_CI_DD_AGENT_URL}"
      riot_cmd=(bin/riot-env/bin/python -m riot -P -v run --pass-env -s)
      for _hash in ${RIOT_HASHES}; do
        "${riot_cmd[@]}" --command \
          'export DD_TRACE_AGENT_URL="$DDTEST_PLAN_AGENT_URL"; export PYTEST_ADDOPTS="$DDTEST_PYTEST_ADDOPTS"; case "$NIGHTLY_BUILD" in true) ;; *) case "$PYTEST_ADDOPTS" in *--no-cov*) ;; *) export PYTEST_ADDOPTS="$PYTEST_ADDOPTS --no-cov" ;; esac ;; esac; ./bin/ddtest plan --platform python --framework pytest --tests-location "$DDTEST_TESTS_LOCATION" --min-parallelism "$DDTEST_NODES" --max-parallelism "$DDTEST_NODES"' \
          "${_hash}"
        mv .testoptimization ".testoptimization-${_hash}"
      done
  artifacts:
    when: always
    paths:
      - .testoptimization-*/
    expire_in: 1 week

# Run: execute this CI node's files in the activated venv.
# 4 workers (not ncpu): the testrunner image runs on many-core k8s pods
# where ncpu spawns too many workers and corrupts pytest-cov's sqlite DB;
# matching PYTEST_XDIST_AUTO_NUM_WORKERS=4 from testrunner.yml.
.ddtest_run:
  extends: .ddtest_base
  script:
    - |
      # Restore this venv's plan artifact (partitioned by RIOT_HASH in the plan job).
      rm -rf .testoptimization
      mv ".testoptimization-${RIOT_HASH}" .testoptimization
      export COVERAGE_FILE=".coverage.${RIOT_HASH}.${CI_NODE_INDEX}"
      riot_cmd=(bin/riot-env/bin/python -m riot -P -v run --pass-env -s)
      # Keep CI Visibility session names tied to the CI job and test command;
      # ddtest expands these placeholders for each local worker.
      "${riot_cmd[@]}" --command \
        'export PYTEST_ADDOPTS="$DDTEST_PYTEST_ADDOPTS"; export DD_TEST_SESSION_NAME="${{CI_JOB_NAME}}-pytest ${{DDTEST_SUITE_PATH}} ${{DDTEST_PYTEST_ADDOPTS}} node-{{{{nodeIndex}}}}-worker-{{{{workerIndex}}}}"; case "$NIGHTLY_BUILD" in true) ;; *) case "$PYTEST_ADDOPTS" in *--no-cov*) ;; *) export PYTEST_ADDOPTS="$PYTEST_ADDOPTS --no-cov" ;; esac ;; esac; export COVERAGE_FILE="$COVERAGE_FILE"; ./bin/ddtest run --platform python --framework pytest --ci-node "$CI_NODE_INDEX" --ci-node-workers 4' \
        "${RIOT_HASH}"
      # Consolidate per-worker coverage files into one .coverage file.
      # Each xdist worker wrote to its own COVERAGE_FILE;
      # 'coverage combine' merges them so coverage can be uploaded.
      coverage_files=(.coverage.*)
      if [[ -e "${coverage_files[0]}" ]]; then
        coverage combine
      fi

.ddtest_run_snapshot:
  extends: .ddtest_base_snapshot
  script:
    - |
      rm -rf .testoptimization
      mv ".testoptimization-${RIOT_HASH}" .testoptimization
      export COVERAGE_FILE=".coverage.${RIOT_HASH}.${CI_NODE_INDEX}"
      riot_cmd=(bin/riot-env/bin/python -m riot -P -v run --pass-env -s)
      # Snapshot suites use --ci-node-workers 1 (not 4): the shared testagent
      # is session-token-keyed, and parallel xdist workers cause cross-session
      # contamination (wrong stats, '400: list index out of range').
      "${riot_cmd[@]}" --command \
        'export PYTEST_ADDOPTS="$DDTEST_PYTEST_ADDOPTS"; export DD_TEST_SESSION_NAME="${{CI_JOB_NAME}}-pytest ${{DDTEST_SUITE_PATH}} ${{DDTEST_PYTEST_ADDOPTS}} node-{{{{nodeIndex}}}}-worker-{{{{workerIndex}}}}"; case "$NIGHTLY_BUILD" in true) ;; *) case "$PYTEST_ADDOPTS" in *--no-cov*) ;; *) export PYTEST_ADDOPTS="$PYTEST_ADDOPTS --no-cov" ;; esac ;; esac; export COVERAGE_FILE="$COVERAGE_FILE"; export DD_TRACE_AGENT_URL="http://testagent:9126"; ./bin/ddtest run --platform python --framework pytest --ci-node "$CI_NODE_INDEX" --ci-node-workers 1' \
        "${RIOT_HASH}"
      # Consolidate per-worker coverage files into one .coverage file.
      coverage_files=(.coverage.*)
      if [[ -e "${coverage_files[0]}" ]]; then
        coverage combine
      fi

# Required jobs will appear here
