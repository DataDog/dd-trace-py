version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
    rust: "1.86"

  commands:
    - |
        RUST_VERSION=`rustc --version | cut -d' ' -f2`
        echo "Resolved RUST_VERSION=$RUST_VERSION"
        export PATH="/home/docs/.asdf/installs/rust/$RUST_VERSION/bin:$PATH"
        rustc --version
