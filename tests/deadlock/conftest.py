"""
pytest configuration for tests/deadlock/.

The global ``deadlock_watchdog`` autouse fixture is defined in
``tests/conftest.py`` and automatically applies to all tests.  This file
exists so pytest can discover the test package correctly.
"""
