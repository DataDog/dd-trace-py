name: Test Requirements Lockfiles Check
on:
  push:
    branches:
      - 1.x
  pull_request:
    types: [opened, reopened, synchronize]
jobs:
  validate:
    name: Check requirements lockfiles
    runs-on: ubuntu-latest
    container: ghcr.io/datadog/dd-trace-py/testrunner:latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Fixup git permissions
        # https://github.com/actions/checkout/issues/766
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Set python interpreters
        run: pyenv global 3.10.3 2.7.18 3.5.10 3.6.15 3.7.13 3.8.13 3.9.11 3.11.0

      - name: Install Dependencies
        run: pip install --upgrade pip && pip install riot

      - name: Generate riot locks
        run: scripts/compile-and-prune-test-requirements

      - name: Check git diff
        run: scripts/check-diff 'Mismatches found between .riot/requirements/*.txt and riotfile.py. Run scripts/compile-and-prune-test-requirements and commit the result.'
