---
features:
  - |
    LLM Observability: Adds support for configuring proxy URLs for LLM Observability using the ``DD_LLMOBS_INSTRUMENTED_PROXY_URLS`` 
    environment variable or by enabling LLM Observability with the ``instrumented_proxy_urls`` argument. Spans sent to a proxy URL
    will now show up as workflow spans instead of LLM spans.
