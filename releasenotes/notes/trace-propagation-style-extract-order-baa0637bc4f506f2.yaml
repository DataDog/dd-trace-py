---
fixes:
  - |
    The specification of ``DD_TRACE_PROPAGATION_STYLE_EXTRACT`` requires the configured styles be evaluated in order. However, the evaluation order had been hard-coded and so ignored the configured ordering.
