---
fixes:
  - |
    tracing: Removes deprecation warning and adds ``ddtrace._trace.Context``. This class can now be used to propagate context across threads and processes.
    This class must be imported from the ``ddtrace`` module (ex: ``from ddtrace import Context``).
