---
fixes:
  - |
    profiling: Fixes an issue where enabling the profiler with gevent workers
    caused gunicorn to skip graceful shutdown, killing in-flight requests
    immediately on ``SIGTERM`` instead of honoring ``--graceful-timeout``.
