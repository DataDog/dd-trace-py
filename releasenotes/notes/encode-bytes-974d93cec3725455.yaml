---
fixes:
  - |
    tracing: This resolves an encoding error when appending a unicode string to a bytes object, which previously resulted in a TypeError. It now appends a bytes literal when truncating a bytes object.