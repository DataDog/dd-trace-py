---
features:
  - |
    openfeature: Datadog now bounds the number, size, and nesting depth of evaluation context
    fields reported with flag evaluations. Context data beyond these limits is omitted from
    the reported evaluation; flag resolution itself is unaffected.
