---
fixes:
  - |
    rq: Fix ``rq.job.Job.fetch_many`` not being instrumented. Calls to
    ``Job.fetch_many`` now produce ``rq.job.fetch_many`` spans with the
    correct service name (respecting ``DD_SERVICE`` like all other rq spans).
