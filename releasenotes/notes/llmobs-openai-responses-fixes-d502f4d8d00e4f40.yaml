---
fixes:
  - |
    openai: fixes an issue where token metrics for streamed reasoning responses from the responses api threw an ``AttributeError``.
  - |
    LLM Observability: fixes an issue where input messages for tool messages were not being captured properly.
