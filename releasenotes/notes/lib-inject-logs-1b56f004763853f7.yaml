---
fixes:
  - |
    lib-injection: changes the log output to system logs rather than stderr. Logging to stderr could interfere with applications. Logs can still be sent to stderr using `DD_TRACE_DEBUG=1`.
