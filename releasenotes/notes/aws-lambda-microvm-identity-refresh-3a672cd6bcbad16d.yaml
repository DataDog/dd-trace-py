---
fixes:
  - |
    AWS Lambda MicroVM: Fixes MicroVM instances on AWS Lambda sharing the same runtime ID and Remote
    Config client ID after resuming from a snapshot, which made distinct instances appear as
    one in traces and telemetry. Only applies to AWS Lambda MicroVM deployments; no behavior
    change for other environments.
