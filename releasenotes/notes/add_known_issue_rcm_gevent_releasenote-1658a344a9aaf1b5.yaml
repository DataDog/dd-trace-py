---
issues:
  - |
    ASM: There is a known issue with remote config management(RCM) when paired with gevent which can cause workers to hang. 
    If you're using gevent and ASM (``DD_APPSEC_ENABLED=true``), we recommend disabling RCM with ``DD_REMOTE_CONFIGURATION_ENABLED=false``.
