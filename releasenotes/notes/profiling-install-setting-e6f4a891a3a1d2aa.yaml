---
features:
  - |
    profiling: Add ``DD_PROFILING_INSTALL`` (default ``false``). Set it to ``true``
    to install profiler patches and signal handlers without starting profiling.
    ``DD_PROFILING_ENABLED`` still controls whether profiling runs.
