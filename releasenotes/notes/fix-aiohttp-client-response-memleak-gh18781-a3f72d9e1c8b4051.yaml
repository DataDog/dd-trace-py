---

fixes:
  - |
    aiohttp: Fixed a memory leak (GH-18781) introduced in ddtrace 4.9 where the aiohttp
    client integration retained a strong reference to each ``ClientResponse`` object,
    preventing GC of response bodies and causing linear RSS growth on sequential requests.
