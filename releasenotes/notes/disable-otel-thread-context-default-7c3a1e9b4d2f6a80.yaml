---
fixes:
  - |
    tracing: Fixes a native memory leak on Linux that increased process RSS over time when
    OpenTelemetry thread-context publication was enabled. ``DD_TRACE_OTEL_CTX_ENABLED`` now
    defaults to ``false``. Set it to ``true`` to restore publication.
