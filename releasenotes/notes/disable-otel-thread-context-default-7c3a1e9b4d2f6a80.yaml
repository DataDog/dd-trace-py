---
upgrade:
  - |
    tracing: ``DD_TRACE_OTEL_CTX_ENABLED`` now defaults to ``false``. Setting it to ``true``
    still enables OpenTelemetry thread-context publication and can still increase process
    memory on Linux when threads exit.
