---
deprecations:
  - |
    tracing: ``DD_ASGI_TRACE_WEBSOCKET`` is deprecated; use ``DD_TRACE_WEBSOCKET_MESSAGES_ENABLED`` instead.
    This variable now emits a ``DDTraceDeprecationWarning`` (previously only a log warning).
