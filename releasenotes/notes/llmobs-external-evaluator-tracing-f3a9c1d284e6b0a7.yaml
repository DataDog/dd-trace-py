---
features:
  - |
    LLM Observability: Adds ``LLMObs.evaluation()``, which traces an external LLM-as-a-judge evaluator
    as a judge span tagged with the span it evaluated. ``LLMObs.submit_evaluation()`` accepts a
    ``judge_span`` argument that links a score to that span.
