fixes:
  - |
    LLM Observability: Fixes an issue where formatting managed text or chat prompts
    consumes a closing JSON object brace immediately after a single-brace placeholder,
    producing malformed JSON. Braces surrounding placeholders are now preserved.
