---
other:
  - |
    profiling: When the stack profiler loses the ``SIGSEGV`` or ``SIGBUS`` handler, it now
    logs which of the two it lost and which component owns it, naming the owning function
    and the binary's build ID where they can be resolved.
