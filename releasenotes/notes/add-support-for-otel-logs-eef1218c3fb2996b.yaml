---
features:
  - |
    opentelemetry: Adds experimental support for exporting logs using the OTLP protocol.
    To enable this feature install the ``opentelemetry-otlp-exporter`` and set ``DD_LOGS_OTEL_ENABLED=true``.