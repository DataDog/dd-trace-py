---
features:
  - |
    Profiling: The heap profiler now tracks allocations made through
    ``PYMEM_DOMAIN_MEM`` (``PyMem_Malloc/Calloc/Realloc``) in addition to the
    existing ``PYMEM_DOMAIN_OBJ`` coverage. This makes previously invisible
    allocations visible in heap profiles.
