---
deprecations:
  - |
    tracing: Internalizes the ``ddtrace.settings.config`` module and deprecates support for the following ``ddtrace.config`` attributes:
    - http
    - http_server
    - trace_headers
    - header_is_traced
    - convert_rc_trace_sampling_rules
    - enable_remote_configuration
    - get_from
