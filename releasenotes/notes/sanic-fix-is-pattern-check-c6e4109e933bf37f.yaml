---
fixes:
  - |
    sanic: Resolves ``sanic_routing.exceptions.InvalidUsage`` error raised when
    gevent is installed and moduling cloning is enabled (``DD_UNLOAD_MODULES_FROM_SITECUSTOMIZE=auto``).
