---
fixes:
  - |
    LLM Observability: Fixes agentless export silently dropping data for customers on the ``us3``,
    ``us5``, ``ap1``, and ``ap2`` Datadog sites. These sites were previously unsupported, causing
    all exported spans and LLM traces to be dropped.
