---
fixes:
  - |
    CI Visibility: Fixes a ``ValueError: tool 1 is already in use`` crash when ddtrace's
    coverage instrumentation and coverage.py's ``SysMonitor`` backend (default on Python 3.14+)
    both claim ``sys.monitoring.COVERAGE_ID``.
