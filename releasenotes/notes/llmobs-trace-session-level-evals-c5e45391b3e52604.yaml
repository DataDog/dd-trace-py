features:
  - |
    LLM Observability: Adds support for submitting trace-level evaluation metrics via
    ``LLMObs.submit_evaluation()``. A new ``eval_scope`` parameter is introduced (one of
    ``"span"`` (default) or ``"trace"``). Use ``eval_scope="trace"`` to associate an evaluation
    with an entire trace by providing the root span.
