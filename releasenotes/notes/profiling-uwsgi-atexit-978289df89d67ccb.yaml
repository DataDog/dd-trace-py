---
fixes:
  - |
    profiling: Profiling won't load if ``--skip-atexit`` is not set when
    ``--lazy`` or ``--lazy-apps`` is set on uWSGI<2.0.30. This is to prevent
    crashes from profiling native extension modules. See
    https://github.com/unbit/uwsgi/pull/2726 for details.

