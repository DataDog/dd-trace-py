---
fixes:
  - |
    CI Visibility: Test Impact Analysis per-test coverage and coverage report upload can
    now run simultaneously. When report upload is enabled, the internal coverage collector
    skips the ``sys.monitoring.DISABLE`` optimisation so it never needs to call
    ``restart_events()`` between tests, leaving coverage.py's monitoring state untouched.
