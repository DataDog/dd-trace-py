---
fixes:
  - |
    CI Visibility: When both Test Impact Analysis and coverage report upload are enabled,
    per-test coverage collection is now explicitly disabled and a warning is logged.
    The two mechanisms cannot run simultaneously on Python 3.12+.
