---
fixes:
  - |
    internal: This fix resolves an issue regarding the remote config module where a payload missing the ``lib_config`` entry would crash the application due to an unhandled ``KeyError`` exception.
