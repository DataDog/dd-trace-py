---
upgrade:
  - |
    LLM Observability: manual instrumentation methods, including ``LLMObs.annotate()``, ``LLMObs.export_span()``, and ``LLMObs.submit_evaluation()`` now raise exceptions instead of logging. LLM Observability auto-instrumentation is not affected.

