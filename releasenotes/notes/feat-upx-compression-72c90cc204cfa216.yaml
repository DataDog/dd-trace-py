---
features:
  - |
    Uses compression (LZMA) to minimize the distributed and on-disk size of
    ELF-based dynamic libraries.  This should have the effect of reducing
    real overall package size by about 30-50%.
