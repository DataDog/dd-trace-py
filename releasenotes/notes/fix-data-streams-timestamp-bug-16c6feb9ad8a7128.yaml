fixes:
  - |
    data_streams: Fix Kafka & SQS integrations. Data Streams was incorrectly setting timestamps for statistics, which lead to all points being submitted for the same timestamp (start of the application).
