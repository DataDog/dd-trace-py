---
other:
  - |
    profiling: Update docs and tests for uwsgi ``--lazy-apps`` config. When this
    is set, we advise our customers to also set ``--skip-atexit`` to avoid
    crashes that could occur from our native extensions when worker processes
    are terminated.
