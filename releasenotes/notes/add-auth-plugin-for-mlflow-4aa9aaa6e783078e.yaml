features:
  - |
    mlflow: Adds a request header provider (auth plugin) for MLFlow. If the environment variables DD_API_KEY and DD_APP_KEY are set, HTTP requests to the MLFlow tracking server will include the DD-API-KEY and DD-APPLICATION-KEY headers.
