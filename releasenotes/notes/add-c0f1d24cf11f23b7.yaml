---
features:
  - |
    - Botocore service name: This adds a feature to override the default service name in botocore
      by either setting the environment variable `DD_BOTOCORE_SERVICE` or setting the service name field in `ddtrace.config`. 
