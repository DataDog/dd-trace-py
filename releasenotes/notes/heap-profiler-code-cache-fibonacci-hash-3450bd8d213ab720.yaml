---
other:
  - |
    profiling: introduce a ``PyCodeObject*`` cache in the heap profiler to
    avoid repeated libdatadog string interning and function-registration
    calls on hot allocation sites.
