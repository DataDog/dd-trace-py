---
deprecations:
  - |
    ``ddtrace.ext.sql.ROWS`` is deprecated. Use ``ddtrace.ext.db.ROWCOUNT`` instead.
  - |
    ``ddtrace.ext.cassandra.ROW_COUNT`` is deprecated. Use ``ddtrace.ext.db.ROWCOUNT`` instead.