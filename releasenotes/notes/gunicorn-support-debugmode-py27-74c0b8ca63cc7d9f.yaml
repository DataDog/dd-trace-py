---
fixes:
  - |
    gunicorn: This fix ensures ddtrace threads do not block execution in gevented workers when ``DD_TRACE_DEBUG=true``. 
    This issue only impacts gunicorn applications using gevent and ``python<=3.6``.
