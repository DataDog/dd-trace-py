---
fixes:
  - |
    opentelemetry: Fixed spans going unsampled when using ``opentelemetry.trace.get_current_span()`` or ``NonRecordingSpan``. Spans are now kept and appear in the UI unless explicitly dropped by the Agent or sampling rules.