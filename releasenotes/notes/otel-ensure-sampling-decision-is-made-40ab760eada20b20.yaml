---
fixes:
  - |
    opentelemetry: Addresses an edge case where distributed tracing headers could be generated before a sampling decision waa made.
    Resulting in dropped spans in downstream services.
