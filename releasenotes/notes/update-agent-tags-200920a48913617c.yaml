---
other:
  - |
    agent: Migrates agent connection configurations to envier. This provides better documentation and validation for expected types. 
    The following configurations are affected: DD_TRACE_AGENT_HOSTNAME, DD_AGENT_HOST, DD_TRACE_AGENT_PORT, DD_AGENT_PORT, DD_TRACE_AGENT_URL
    DD_DOGSTATSD_HOST, DD_AGENT_HOST, DD_DOGSTATSD_PORT, DD_AGENT_PORT, DD_DOGSTATSD_URL, and DD_TRACE_AGENT_TIMEOUT_SECONDS.
