---
fixes:
  - |
    litellm: Fix missing LLMObs spans when routing requests through a litellm
    proxy. Proxy requests were incorrectly suppressed by the OpenAI downstream
    span check, resulting in empty or missing LLMObs spans. Proxy requests are
    now always handled by the litellm integration.
