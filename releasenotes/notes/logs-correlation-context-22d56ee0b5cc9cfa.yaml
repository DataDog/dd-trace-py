---
features:
  - |
    The ``ddtrace.contrib.trace_utils.get_logs_correlation_context`` method has been added to replace
    ``ddtrace.helpers.get_correlation_ids``. It now returns a ``DDLogRecord`` which includes the current span's
    trace and span ids, as well as the configured service, version, and env names.
