---
fixes:
  - |
    Solve some corner cases where a Flask blocking request would fail because 
    headers would be already sent.
  - |
    Solve the content-type not always being correct in blocking responses.
  - |
    Ensure the blocking responses have some span tags.
