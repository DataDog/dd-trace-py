fixes:
  - |
    CI Visibility: Fixes an issue where code coverage path aliases from the
    active coverage configuration were not applied to LCOV source filenames
    when the mapped destination files did not exist on disk.  This caused
    uploaded coverage reports to retain separate installed-package paths for
    the same repository file instead of deduplicating them to the canonical
    repository path.
