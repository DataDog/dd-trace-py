---
fixes:
  - |
    LLM Observability: This fix resolves an issue where successive calls to LLMObs.annotate on the same span caused complete 
          overwriting of metrics, metadata, and prompt dictionaries instead of merging the new values with existing ones.