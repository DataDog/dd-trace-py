---
fixes:
  - |
    ASM: Fixes an issue where Exploit Prevention does not evaluate SSRF rules for calls to
    ``webbrowser.open``, or for calls to ``urllib.request.urlopen`` that pass the URL
    positionally or go through a custom opener installed with ``urllib.request.install_opener``.
