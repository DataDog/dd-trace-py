---
fixes:
  - |
    langchain: This fix resolves a problem when formatting the api_key when the api_key is a 
    pydantic `SecretStr` type. By calling `get_secret_value()` on the `SecretStr` type before
    doing the formatting.
    