---
fixes:
  - |
    redis: Updates the resource name of redis spans the only include the command (ex: SET/GET/PUSH/....).
    To revert to the previous behavior set ``DD_REDIS_RESOURCE_ONLY_COMMAND`` to ``FALSE``.