---
fixes:
  - |
    tracing: Fixes an issue where agentless trace export dropped every span in a trace if any span
    carried a value that is not JSON serializable, such as a ``uuid.UUID`` recorded in LLM
    Observability span metadata. Such values are now serialized to their string representation.
