---
fixes:
  - |
    LLM Observability: Resolves a ``TypeError`` that dropped all spans in a trace when span metadata
    contained a value that is not JSON serializable, such as a ``uuid.UUID``. Affects agentless mode only.
