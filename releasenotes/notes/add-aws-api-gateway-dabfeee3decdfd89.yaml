---
features:
  - |
    API Gateway: Add preview support to create inferred AWS API Gateway spans when certain AWS Gateway headers are passed into the request. 
    Requires ``DD_TRACE_INFERRED_PROXY_SERVICES_ENABLED=true`` to enable. For enablement instructions, `contact support. <https://docs.datadoghq.com/help/>`_
