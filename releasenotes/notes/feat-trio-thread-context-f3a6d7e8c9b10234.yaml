features:
  - |
    trio: Adds native trace-context support for Trio tasks and worker threads on Linux. The
    integration is enabled by default and can be disabled with ``DD_TRACE_TRIO_ENABLED=false``.
