---
other:
  - |
    LLM Observability: when LLMObs is enabled in agentless mode (Datadog Agent not reachable or with ``DD_LLMOBS_AGENTLESS_ENABLED=1``),
    APM traces are now exported agentlessly to Datadog's intake. However, ``DD_APM_TRACING_ENABLED=false`` being set takes higher
    precedence and LLMObs span events will continue to ship separately as existing behavior.
