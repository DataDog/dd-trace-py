---
features:
  - |
    openfeature: Hashes targeting keys and omits evaluation context from experimental server-side
    evaluation telemetry by default. Raw targeting keys and bounded evaluation context are reported
    only when full evaluation data is enabled by the delivered feature-flag configuration.
