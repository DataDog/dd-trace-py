---
features:
  - |
    openfeature: Feature flag evaluation telemetry now hashes targeting keys and omits context by default;
    enabling full evaluation data for the environment includes raw targeting keys and bounded context.
    In both modes, errors contain only standard error codes and invalid targeting keys are omitted without
    losing evaluation counts.
