---
features:
  - |
    openfeature: Feature flag reports sent to Datadog now hash user identifiers and exclude
    user attributes by default to reduce the collection of personal data, without changing
    flag results. Enabling full evaluation data for an environment includes the original
    identifiers and attributes; detailed error messages are never included.
