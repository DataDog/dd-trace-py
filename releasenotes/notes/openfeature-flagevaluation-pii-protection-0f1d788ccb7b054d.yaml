---
features:
  - |
    openfeature: Protects experimental server-side evaluation telemetry by hashing targeting keys and
    omitting evaluation context by default, while retaining raw targeting keys and bounded context when
    full evaluation data is enabled in the delivered feature-flag configuration.
