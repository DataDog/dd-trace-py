---
fixes:
  - |
    AI Guard: Fixes an issue where credentials embedded in ``DD_AI_GUARD_ENDPOINT`` could be
    written to debug logs, exception messages and span error tags when an evaluation failed to
    reach the AI Guard service. Any user info in the endpoint is now removed before the request is
    sent, and the endpoint is redacted from failure messages.
