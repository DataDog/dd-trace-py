---
features:
  - |
    LLM Observability: The ``claude_agent_sdk`` integration now emits span links between
    step, LLM, and tool spans, enabling the Execution Graph visualization for
    ``claude_agent_sdk`` traces.
