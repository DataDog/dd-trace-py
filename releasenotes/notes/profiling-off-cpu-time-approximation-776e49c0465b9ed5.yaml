---
features:
  - |
    Profiling: Adds off-CPU time as a new sample type in the stack profiler.
    Each stack sample now includes an ``off-cpu-time`` value approximating the
    time a thread spent waiting (on I/O, locks, sleeps, or OS scheduling).
