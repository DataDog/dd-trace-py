---
features:
  - |
    Profiling: Adds an off-CPU time approximation of the time a thread spent waiting
    on I/O, locks, sleeps, or OS scheduling.
    It is surfaced as a new sample type in the stack profiler, temporarily named
    ``experimental-nanoseconds`` (to be renamed as ``off-cpu-time`` when the feature is GA'ed).
