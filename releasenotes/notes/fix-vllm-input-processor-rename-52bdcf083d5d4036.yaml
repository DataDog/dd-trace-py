---
fixes:
  - |
    vllm: Fixes an issue where the integration failed to instrument vLLM >= 0.14.0
    with ``ModuleNotFoundError: No module named 'vllm.v1.engine.processor'``,
    silently disabling all vLLM tracing and metrics.
