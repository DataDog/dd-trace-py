---
fixes:
  - |
    tracing: Fixes an issue where OpenTelemetry thread-context record publication could not be disabled on Linux.
    Publication can now be disabled by setting the experimental ``DD_TRACE_OTEL_CTX_ENABLED`` environment variable to ``false``.
