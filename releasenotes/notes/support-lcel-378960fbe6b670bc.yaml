---
features:
  - |
    This adds tracing support for LangChain's LCEL (LangChain Expression Language) chaining syntax.
    This change specifically adds tracing support for the `invoke` and `batch` functions, as well as their `async` versions.
    This change does not include tracing support for the `stream` function.
