---
features:
  - |
    LLM Observability: the ``pydantic_ai`` integration now captures a richer agent manifest on each
    ``kind="agent"`` span, additionally recording the agent's model provider, dynamic instruction and
    system-prompt resolvers, capabilities (sub-agents, builtin tools, MCP servers, and custom toolsets),
    handoffs, guardrails, output type, memory policies, tool transforms, and settings.
