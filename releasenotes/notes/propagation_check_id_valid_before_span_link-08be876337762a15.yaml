---
fixes:
  - |
    propagation: This fix resolves an issue where a ``Context`` generated from extracted headers could
    lack a span_id or trace_id, leading to an encoding error for ``SpanLink``s.
