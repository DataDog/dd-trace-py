---
fixes:
  - |
    tracing: Fixes an issue where HTTP client spans from ``aiohttp``, ``httplib``,
    ``httpx``, and ``urllib3`` are grouped under a generic operation name instead of
    by HTTP method and URL path.
