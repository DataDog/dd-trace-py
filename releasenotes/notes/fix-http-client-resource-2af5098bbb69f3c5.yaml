---
fixes:
  - |
    tracing: When ``DD_TRACE_OTEL_SEMANTICS_ENABLED=true``, HTTP client spans now use
    the HTTP method as their resource name, reducing resource cardinality. Default
    resource names are unchanged.
