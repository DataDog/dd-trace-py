---
other:
  - |
    profiling: Each uploaded profile now exposes the effective profiler configuration
    as ``dd.profiling.*`` keys in the upload event's ``info`` field, so profiles can be
    filtered by individual settings.
