---
fixes:
  - |
    tracing: Fixes a ``TypeError: cannot pickle '_thread.lock'`` raised when
    serializing ddtrace-instrumented objects with ``cloudpickle``.
    Fork-safe locks and events (``ddtrace.internal.forksafe.Lock`` /
    ``Event``) are being properly wrapped and unwrapped now, and are pickle-safe.
