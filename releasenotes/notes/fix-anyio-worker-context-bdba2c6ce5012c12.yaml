features:
  - |
    anyio: Adds trace context propagation to synchronous FastAPI handlers that run in
    AnyIO worker threads. The integration is enabled by default and can be disabled with
    ``DD_TRACE_ANYIO_ENABLED=false``.
