features:
  - |
    anyio: Adds native trace-context support for synchronous FastAPI handlers running in
    AnyIO worker threads on Linux. The integration is enabled by default and can be disabled
    with ``DD_TRACE_ANYIO_ENABLED=false``.
