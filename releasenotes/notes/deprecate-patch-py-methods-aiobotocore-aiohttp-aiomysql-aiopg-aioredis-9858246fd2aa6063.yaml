---
deprecations:
  - |
    aiobotocore: All methods in aiobotocore/patch.py except ``patch()`` and ``unpatch()`` are deprecated and will be removed in version 3.0.0.
  - |
    aiohttp: All methods in aiohttp/patch.py except ``patch()`` and ``unpatch()`` are deprecated and will be removed in version 3.0.0.
  - |
    aiohttp_jinja2: All methods in aiohttp_jinja2/patch.py except ``patch()`` and ``unpatch()`` are deprecated and will be removed in version 3.0.0.
  - |
    aiomysql: All methods in aiomysql/patch.py except ``patch()`` and ``unpatch()`` are deprecated and will be removed in version 3.0.0.
  - |
    aiopg: All methods in aiopg/patch.py except ``patch()`` and ``unpatch()`` are deprecated and will be removed in version 3.0.0.
  - |
    aioredis: All methods in aioredis/patch.py except ``patch()`` and ``unpatch()`` are deprecated and will be removed in version 3.0.0.