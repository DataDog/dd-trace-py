fixes:
  - |
    django: This fix resolves an issue where 'span.resource' would not be contain the endpoint when a Handler was interrupted, such as in the case
    of timeouts with gunicorn workouts.  