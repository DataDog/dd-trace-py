---
features:
  - |
     LLM Observability: Introduces the `LLMObs.annotation_context()` context manager method which allows modifying or overriding the tags of integration generated LLM Observability spans created while the conext manager is active.