pull_request_rules:
  - name: automatic merge
    conditions:
      - label=automerge
    actions:
      merge:
        strict: smart
        method: squash
  - name: delete head branch on merge
    conditions:
      - merged
    actions:
      delete_head_branch: {}
