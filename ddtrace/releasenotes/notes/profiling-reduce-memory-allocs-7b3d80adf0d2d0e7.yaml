---
fixes:
  - |
    profiling: fixes an issue where the profiler was allocating too much memory
    from ``ensure_binary_or_empty()`` function, on Python versions before 3.12,
    with ``DD_PROFILING_EXPORT_LIBDD_ENABLED`` or ``DD_PROFILING_TIMELINE_ENABLED``.
