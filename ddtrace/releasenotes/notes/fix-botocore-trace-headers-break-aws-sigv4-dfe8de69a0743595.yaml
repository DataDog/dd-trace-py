---
fixes:
  - |
    botocore, aiobotocore: Resolves an issue where outbound AWS SDK
    requests to strict-signature endpoints (e.g. AppSync, API Gateway
    with IAM authorization) were rejected with ``The request signature
    we calculated does not match the signature you provided`` when
    distributed tracing was enabled.
