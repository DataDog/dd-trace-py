cmake_minimum_required(VERSION 3.19)

# Tests for memalloc profiler TODO: Add tests for the heap profiler directly

# Note: test_memalloc_heap_map was removed as the wrapper class was eliminated in favor of using the underlying map
# directly in _memalloc_heap.cpp
