"""Publish thread context for synchronous AnyIO workers.

The integration is enabled automatically with ``ddtrace-run`` or
``import ddtrace.auto``. It can also be enabled with ``patch(anyio=True)``.
"""
