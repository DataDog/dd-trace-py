"""Disabled implementation for the ddtrace pytest-bdd plugin."""

# The pytest_bdd plugin doesn't define any pytest hooks itself
# It only adds configuration when enabled, so no hooks needed when disabled
pass
