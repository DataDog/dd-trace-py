[package]
name = "ddtrace-native"
version = "0.1.0"
edition = "2021"

[profile.release]
lto = true
strip = "debuginfo"
opt-level = 3

[dependencies]
pyo3 = { version = "0.24.0", features = ["extension-module", "abi3-py38"] }
datadog-ddsketch = { git = "https://github.com/DataDog/libdatadog", rev = "e81d920" }
datadog-library-config = { git = "https://github.com/DataDog/libdatadog", rev = "e81d920" }
data-pipeline = { git = "https://github.com/DataDog/libdatadog", rev = "e81d920" }
tinybytes = { git = "https://github.com/DataDog/libdatadog", rev = "e81d920" }
ddcommon = { git = "https://github.com/DataDog/libdatadog", rev = "e81d920" }
# datadog-ddsketch = {path = "../../../libdatadog/ddsketch"}
# datadog-library-config = {path="../../../libdatadog/datadog-library-config"}
# data-pipeline = {path = "../../../libdatadog/data-pipeline"}
# tinybytes = {path = "../../../libdatadog/tinybytes"}
# ddcommon = {path = "../../../libdatadog/ddcommon"}


[build-dependencies]
pyo3-build-config = "0.24"


[lib]
name = "_native"
path = "lib.rs"
crate-type = ["cdylib"]


[net]
# Use git binary from the system instead of the built-in git client
# "Setting this to true can be helpful if you have special authentication requirements that Cargo does not support."
git-fetch-with-cli = true
