[package]
name = "ddtrace-native"
version = "0.1.0"
edition = "2021"
resolver = "2"

[profile.release]
lto = true
debug = "line-tables-only"
opt-level = 's'
codegen-units = 1

[features]
crashtracker = ["dep:anyhow", "dep:libdd-crashtracker"]
profiling = ["dep:libdd-profiling-ffi"]

[dependencies]
anyhow = { version = "1.0", optional = true }
datadog-ffe = { git = "https://github.com/DataDog/libdatadog", branch = "jwiriath/antithesis-poc", version = "1.0.0", features = ["pyo3"] }
libdd-crashtracker = { git = "https://github.com/DataDog/libdatadog", branch = "jwiriath/antithesis-poc", optional = true }
libdd-ddsketch = { git = "https://github.com/DataDog/libdatadog", branch = "jwiriath/antithesis-poc" }
libdd-library-config = { git = "https://github.com/DataDog/libdatadog", branch = "jwiriath/antithesis-poc" }
libdd-log = { git = "https://github.com/DataDog/libdatadog", branch = "jwiriath/antithesis-poc" }
libdd-data-pipeline = { git = "https://github.com/DataDog/libdatadog", branch = "jwiriath/antithesis-poc" }
libdd-profiling-ffi = { git = "https://github.com/DataDog/libdatadog", branch = "jwiriath/antithesis-poc", optional = true, features = [
    "cbindgen",
] }
libdd-common = { git = "https://github.com/DataDog/libdatadog", branch = "jwiriath/antithesis-poc" }
pyo3 = { version = "0.25", features = ["extension-module", "anyhow"] }
tracing = { version = "0.1", default-features = false }

[build-dependencies]
pyo3-build-config = "0.25"
build_common = { git = "https://github.com/DataDog/libdatadog", branch = "jwiriath/antithesis-poc", features = [
    "cbindgen",
] }

[lib]
name = "_native"
path = "lib.rs"
crate-type = ["cdylib"]

[net]
# Use git binary from the system instead of the built-in git client
# "Setting this to true can be helpful if you have special authentication requirements that Cargo does not support."
git-fetch-with-cli = true
